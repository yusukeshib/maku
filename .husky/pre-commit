#!/bin/sh

# Run Prettier format check
echo "Checking code formatting with Prettier..."
bun run format || exit 1

# Run ESLint
echo "Running ESLint..."
cd apps/editor && bun run lint || exit 1

echo "All pre-commit checks passed!"
